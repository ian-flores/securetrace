% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/otlp.R
\name{otlp_exporter}
\alias{otlp_exporter}
\title{OTLP JSON Exporter}
\usage{
otlp_exporter(
  endpoint = "http://localhost:4318",
  headers = list(),
  service_name = "r-agent",
  batch_size = 100L
)
}
\arguments{
\item{endpoint}{OTLP HTTP endpoint URL (default \code{"http://localhost:4318"}).}

\item{headers}{Named list of HTTP headers to include in requests
(e.g., authentication tokens).}

\item{service_name}{Service name reported in the resource attributes
(default \code{"r-agent"}).}

\item{batch_size}{Maximum number of spans per export batch
(default \code{100L}). Currently unused; reserved for future batching support.}
}
\value{
An S7 \code{securetrace_exporter} object.
}
\description{
Creates an exporter that converts traces to OpenTelemetry Protocol (OTLP)
JSON format and sends them to an OTLP-compatible collector such as
Jaeger, Grafana Tempo, or any OpenTelemetry Collector.
}
\examples{
\dontrun{
# Export to a local Jaeger instance
exp <- otlp_exporter("http://localhost:4318")

tr <- Trace$new("my-run")
tr$start()
s <- Span$new("llm-call", type = "llm")
s$start()
s$set_model("gpt-4o")
s$set_tokens(input = 100L, output = 50L)
s$end()
tr$add_span(s)
tr$end()
export_trace(exp, tr)
}
}
