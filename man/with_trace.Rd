% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{with_trace}
\alias{with_trace}
\title{Execute Code Within a Trace}
\usage{
with_trace(name, expr, ..., exporter = NULL)
}
\arguments{
\item{name}{Name for the trace.}

\item{expr}{Expression to evaluate.}

\item{...}{Additional arguments passed to \code{Trace$new()} as metadata.}

\item{exporter}{Optional exporter. If \code{NULL}, uses the default exporter
(if set).}
}
\value{
The result of evaluating \code{expr}.
}
\description{
Creates a new trace, evaluates the expression, ends the trace, and
optionally exports it. The trace is available via \code{\link[=current_trace]{current_trace()}}
during evaluation.
}
\section{Thread Safety}{

The context stack is process-global, following R's standard single-threaded
assumption. Parallel workers spawned via \pkg{future}, \pkg{callr}, or
\pkg{parallel} receive isolated copies of the stack, so spans created in
those workers will \strong{not} appear in the parent trace. This is consistent
with how \code{options()}, \code{par()}, and \code{Sys.setenv()} behave in
base R.
}

\examples{
# Trace a block of code
result <- with_trace("my-operation", {
  Sys.sleep(0.01)
  1 + 1
})
result

# With an exporter
result <- with_trace("traced-op", {
  10 * 2
}, exporter = console_exporter(verbose = FALSE))
}
