% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exporter.R
\name{exporter_multi}
\alias{exporter_multi}
\alias{multi_exporter}
\title{Multi-Exporter}
\usage{
exporter_multi(...)

multi_exporter(...)
}
\arguments{
\item{...}{For \code{exporter_multi()}: exporter objects to combine.
For \code{multi_exporter()} (deprecated): passed to \code{\link[=exporter_multi]{exporter_multi()}}.}
}
\value{
An S7 \code{securetrace_exporter} object.
}
\description{
Combines multiple exporters into one. When a trace is exported,
it is sent to all contained exporters.
}
\examples{
# Export to both console and JSONL
tmp <- tempfile(fileext = ".jsonl")
combined <- exporter_multi(
  exporter_console(verbose = FALSE),
  exporter_jsonl(tmp)
)

tr <- Trace$new("multi-demo")
tr$start()
tr$end()
export_trace(combined, tr)
unlink(tmp)
}
