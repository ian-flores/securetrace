% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exporter.R
\name{json_stdout_exporter}
\alias{json_stdout_exporter}
\title{JSON stdout Exporter}
\usage{
json_stdout_exporter()
}
\value{
An S7 \code{securetrace_exporter} object.
}
\description{
Creates an exporter that writes one structured JSON line per span to
standard output. This is useful for cloud-native environments where
structured logs are collected from stdout by a log aggregator
(e.g., Fluentd, Vector, CloudWatch Logs).
}
\details{
Each line is a self-contained JSON object containing span-level fields:
\code{trace_id}, \code{span_id}, \code{parent_id}, \code{name}, \code{type}, \code{start_time},
\code{end_time}, \code{status}, \code{duration_secs}, plus any token/cost data and
attributes.
}
\examples{
exp <- json_stdout_exporter()

tr <- Trace$new("demo")
tr$start()
s <- Span$new("step1", type = "tool")
s$start()
s$end()
tr$add_span(s)
tr$end()
export_trace(exp, tr)
}
