% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orchestr.R
\name{trace_agent}
\alias{trace_agent}
\title{Trace an orchestr Agent Invocation}
\usage{
trace_agent(agent, prompt, ..., exporter = NULL)
}
\arguments{
\item{agent}{An orchestr \code{Agent} object.}

\item{prompt}{Character string prompt to send to the agent.}

\item{...}{Additional arguments passed to \code{agent$invoke()}.}

\item{exporter}{Optional exporter for the trace. If \code{NULL}, uses the default
exporter (if set via \code{\link[=set_default_exporter]{set_default_exporter()}}).}
}
\value{
The agent's text response.
}
\description{
Wraps an orchestr \code{Agent}'s \verb{$invoke()} call with automatic tracing.
Creates a span for the agent invocation and auto-extracts model and token
information from the underlying chat object when available.
Requires the orchestr package.
}
\examples{
\dontrun{
# Requires orchestr and ellmer packages
chat <- ellmer::chat_openai(model = "gpt-4o")
ag <- orchestr::agent("assistant", chat)
response <- trace_agent(ag, "What is 2 + 2?")
}
}
