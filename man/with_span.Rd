% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{with_span}
\alias{with_span}
\title{Execute Code Within a Span}
\usage{
with_span(name, type = "custom", expr, ...)
}
\arguments{
\item{name}{Name for the span.}

\item{type}{Span type. One of "llm", "tool", "guardrail", "custom".}

\item{expr}{Expression to evaluate.}

\item{...}{Additional arguments stored as metadata on the span.}
}
\value{
The result of evaluating \code{expr}.
}
\description{
Creates a new span within the current trace, evaluates the expression,
and ends the span. The span is available via \code{\link[=current_span]{current_span()}}
during evaluation.
}
\section{Thread Safety}{

The context stack is process-global, following R's standard single-threaded
assumption. Parallel workers spawned via \pkg{future}, \pkg{callr}, or
\pkg{parallel} receive isolated copies of the stack, so spans created in
those workers will \strong{not} appear in the parent trace. This is consistent
with how \code{options()}, \code{par()}, and \code{Sys.setenv()} behave in
base R.
}

\examples{
# Use with_span inside a trace
with_trace("example", {
  result <- with_span("compute", type = "tool", {
    sqrt(144)
  })
  result
})
}
