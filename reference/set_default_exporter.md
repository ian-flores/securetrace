# Set the Default Exporter

Set the Default Exporter

## Usage

``` r
set_default_exporter(exporter)
```

## Arguments

- exporter:

  An S3 `securetrace_exporter` object.

## Value

Invisible `NULL`.
